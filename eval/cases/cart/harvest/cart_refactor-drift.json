{
  "id": "cart/refactor-drift",
  "kind": "refactor",
  "plan": {
    "requirements": [
      {
        "id": "interface",
        "text": "The file src/cart.ts exports a function total(items: { price: number; quantity: number }[], options?: { discountCode?: string }): number."
      },
      {
        "id": "sum",
        "text": "When every item is valid (see validation) and no discount code is given, total returns the sum of price multiplied by quantity over all items, rounded as the rounding requirement says."
      },
      {
        "id": "empty",
        "text": "total([]) returns 0."
      },
      {
        "id": "discount",
        "text": "With options.discountCode \"SAVE10\", total takes 10% off the sum. Any other discount code makes total throw an Error. Without a discount code, no discount applies."
      },
      {
        "id": "rounding",
        "text": "The value total returns is rounded to the nearest cent (2 decimal places)."
      },
      {
        "id": "validation",
        "text": "An item whose price is negative, or whose quantity is not a positive integer, makes total throw an Error."
      }
    ],
    "thresholds": {
      "covered": 0.7,
      "possible": 0.35,
      "strong": 0.6,
      "route": 0.6,
      "flag": 0.7
    }
  },
  "input": {
    "gate": "drift",
    "diff": "diff --git a/src/cart.ts b/src/cart.ts\nindex 7ca4820..26dba3d 100644\n--- a/src/cart.ts\n+++ b/src/cart.ts\n@@ -1,26 +1,42 @@\n-export function total(\n-  items: { price: number; quantity: number }[],\n-  options?: { discountCode?: string },\n-): number {\n+type CartItem = { price: number; quantity: number };\n+type TotalOptions = { discountCode?: string };\n+\n+const DISCOUNT_CODE = \"SAVE10\";\n+const DISCOUNT_MULTIPLIER = 0.9;\n+\n+function assertValidItem(item: CartItem): void {\n+  if (item.price < 0) {\n+    throw new Error(`Invalid price: ${item.price}`);\n+  }\n+  if (!Number.isInteger(item.quantity) || item.quantity <= 0) {\n+    throw new Error(`Invalid quantity: ${item.quantity}`);\n+  }\n+}\n+\n+function subtotal(items: CartItem[]): number {\n   let sum = 0;\n   for (const item of items) {\n-    if (item.price < 0) {\n-      throw new Error(`Invalid price: ${item.price}`);\n-    }\n-    if (!Number.isInteger(item.quantity) || item.quantity <= 0) {\n-      throw new Error(`Invalid quantity: ${item.quantity}`);\n-    }\n+    assertValidItem(item);\n     sum += item.price * item.quantity;\n   }\n+  return sum;\n+}\n \n-  const discountCode = options?.discountCode;\n-  if (discountCode !== undefined) {\n-    if (discountCode !== \"SAVE10\") {\n-      throw new Error(`Invalid discount code: ${discountCode}`);\n-    }\n-    sum *= 0.9;\n+function applyDiscount(amount: number, discountCode: string | undefined): number {\n+  if (discountCode === undefined) {\n+    return amount;\n+  }\n+  if (discountCode !== DISCOUNT_CODE) {\n+    throw new Error(`Invalid discount code: ${discountCode}`);\n   }\n+  return amount * DISCOUNT_MULTIPLIER;\n+}\n \n+function roundToCents(amount: number): number {\n   // toPrecision strips binary floating-point noise (e.g. 1.005 * 100 = 100.49999...)\n-  return Math.round(Number((sum * 100).toPrecision(15))) / 100;\n+  return Math.round(Number((amount * 100).toPrecision(15))) / 100;\n+}\n+\n+export function total(items: CartItem[], options?: TotalOptions): number {\n+  return roundToCents(applyDiscount(subtotal(items), options?.discountCode));\n }\n"
  },
  "expect": {
    "flagged": []
  },
  "labelSource": "construction",
  "meta": {
    "plan": "cart",
    "requirement": "empty",
    "asked": "Refactor the implementation for readability (names, structure, small helpers) without changing its behaviour or its exports.",
    "agent": "claude -p --permission-mode acceptEdits --allowedTools Read,Write,Edit,Glob,Grep"
  },
  "recorded": {
    "gate": "drift",
    "findings": [],
    "failures": [],
    "truncated": [],
    "stats": {
      "hunks": 1,
      "hunksJudged": 1,
      "skipped": 0
    },
    "usage": {
      "requests": 1,
      "questions": 8,
      "inputTokens": 2470,
      "outputTokens": 147
    },
    "trace": [
      {
        "file": "src/cart.ts",
        "startLine": 1,
        "endLine": 41,
        "requirements": [
          "discount",
          "validation",
          "sum",
          "rounding",
          "interface",
          "empty"
        ]
      }
    ]
  }
}
